//- Self-contained robot card, the counterpart of the main section's photo
//- panel: fills its column in the section's inner grid
#robot-arm-container.relative.overflow-hidden.w-full(
  class='h-[28rem] sm:h-[32rem]'
)
  #robot-arm-stage.absolute.inset-0

script(src=require('../../../scripts/robot.js'))
